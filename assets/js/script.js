document.addEventListener('DOMContentLoaded', function () {
    console.log('Electronic E-Commerce Store loaded');
});

function confirmAction(message) {
    return confirm(message || 'Are you sure?');
}
